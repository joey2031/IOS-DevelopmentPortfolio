//
//  Extentions.swift
//  Bitcoin_App
//
//

//import Foundation
/*
 This shouldnt bee needed, never got to make sure since I could not get it to work
import UIKit
extension Data {
    // this might not be right, we might not even need this.
    func removeJsonPFromBitCoinAPI() -> Data? {
        let str = String(data: self, encoding: .utf8)
        if let str = str {
            let startIndex = str.index(str.startIndex, offsetBy: 39)
            let endIndex = str.index(str.endIndex, offsetBy: -2)
            let newString = str[startIndex..<endIndex]
            return String(newString).data(using: .utf8)
        }
        return nil
    }
}




 Profs original code
extension Data {
    func removeJsonPFromYahooAPI() -> Data? {
        let str = String(data: self, encoding: .utf8)
        if let str = str {
            let startIndex = str.index(str.startIndex, offsetBy: 39)
            let endIndex = str.index(str.endIndex, offsetBy: -2)
            let newString = str[startIndex..<endIndex]
            return String(newString).data(using: .utf8)
        }
        return nil
    }
}
*/
